# Host-side CLI only (apply-scheduling, seed scripts). Copy to .env.local — not used inside Docker.
# Docker compose sets DATABASE_URL=...@postgres:5432/... automatically.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@127.0.0.1:5432/clinic_ops
