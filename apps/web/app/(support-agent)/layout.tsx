import { SupportChrome } from "@/components/layout/SupportChrome";

export default function SupportAgentLayout({ children }: { children: React.ReactNode }) {
  return <SupportChrome>{children}</SupportChrome>;
}

