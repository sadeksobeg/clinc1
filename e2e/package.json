{
  "name": "clinic-os-e2e",
  "private": true,
  "description": "Playwright smoke tests for critical Clinic OS journeys (login + nurse board + appointments).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
